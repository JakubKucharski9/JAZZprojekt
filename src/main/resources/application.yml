spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/student
    username: student
    password: student
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
